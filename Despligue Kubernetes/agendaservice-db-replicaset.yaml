apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: agendaservice-db-replicaset
  labels:
    app: agendaservice-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agendaservice-db
  template:
    metadata:
      labels:
        app: agendaservice-db
    spec:
      containers:
        - name: agendaservice-db
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: common-secrets
                  key: DB_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: common-configmap
                  key: AGENDA_DB_NAME
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: common-secrets
                  key: AGENDA_DB_USERNAME
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: common-secrets
                  key: AGENDA_DB_PASSWORD
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: agendaservice-db-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: agendaservice-db-storage
          persistentVolumeClaim:
            claimName: agendaservice-db-pvc
